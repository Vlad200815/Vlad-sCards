import 'package:flutter/material.dart';

class WordsPackesScreen extends StatelessWidget {
  const WordsPackesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Words Packes Screen"),
      ),
    );
  }
}
